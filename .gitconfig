[core]
	editor = emacs

[alias]
	graph = log --graph --date=short --format=\"%C(yellow)%h%C(reset) %C(magenta)[%ad]%C(reset)%C(auto)%d%C(reset) %s %C(cyan)@%an%C(reset)\"
	co = checkout
	st = status
	df = diff
	cmt = commit
	rebi = rebase -i
[http]
	sslVerify = false
